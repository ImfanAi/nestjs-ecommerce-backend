export class CallbackBody {
    // deposit type
    depositType: string;

    // network type
    network: string;

    // token type
    coin: string;

    // received so far!
    received: number;

    // status
    status: number;
}
